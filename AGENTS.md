# 项目协作规则

- 每次新对话或接手本项目时，必须先读取项目根目录的 `MEMORY.md`，再开始修改任何项目文件。
- 修改前结合当前代码核实记录中的背景和约定；记录与实际状态不一致时，以核实后的状态为准。
- 每次完成项目改动后，必须同步更新 `MEMORY.md` 的改动记录，写明日期、改动内容、涉及文件、验证结果和未完成事项（如有）。
- 保留已有历史记录，新记录追加在改动记录末尾。不要把推测写成已完成的事实，也不要记录密码、API Key 等敏感信息。
