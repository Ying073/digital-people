# 项目记忆与改动记录

## 开始工作前必读

每次新对话或接手本项目时，先完整读取本文件，再开始修改。完成改动后及时补充记录，方便后续对话接续工作。项目根目录的 `AGENTS.md` 同时声明了这一协作要求。

## 项目背景

- 项目名称：西西编程伙伴。
- 项目用途：面向小学 C++ 启蒙课堂的卡通数字人网页应用，支持教材检索、问答、教师管理及语音播放。
- 启动入口：`./run.ps1`；后台启动：`./run.ps1 -Background`。
- 主要目录：`app/` 为应用代码，`data/` 为本地数据，`tests/` 为测试。
- 上述背景依据当前 `README.md` 整理；具体实现和配置须结合当前文件核实。

## 记录约定

- 按日期追加记录，说明改动目的、涉及文件、验证结果及未完成事项。
- 明确区分已完成、未验证和待处理内容。
- 不在本文件中保存密钥、密码或私人数据。
- 本记录自 2026-09-15 起建立；此前改动未逐项追溯。

## 改动记录

### 2026-09-15：建立项目记忆与交接规则

- 需求：在项目内记录改动，并要求新对话先读取记录再开始改动。
- 新增 `MEMORY.md`：整理项目背景，建立后续改动记录格式。
- 新增 `AGENTS.md`：要求开始修改前读取本文件，并在完成改动后同步追加记录。
- 验证：已核对 `README.md` 中的项目背景及启动入口；已检查两份文档内容。本次仅新增文档，未运行应用测试。
- 未完成事项：无。

### 2026-09-15：放慢空闲动作并校准面部效果

- 空闲首次触发和每次动作结束后的等待统一为随机 15–25 秒，移除语音结束及走动后的短间隔特例；眨眼保持独立的 3.2–6 秒等待和 170 毫秒闭眼。
- `app/static/app.js` 为六张实际使用的姿势素材配置原图坐标，别名按素材复用；用原图邻近肤色生成眼睛和嘴部遮罩，闭眼保留眼镜。面部效果按当前显示图片切换，并按 object-fit 的实际显示范围定位，随同人物镜像。
- `app/static/index.html`、`app/static/styles.css` 更新面部图层；姿势切换期间隐藏效果，减少动态效果时关闭面部动画。修复切换时的眨眼结束计时器残留，页面隐藏时停止调度。
- `tests/avatar_runtime.cjs` 新增实际 JS 调度/切换逻辑的模拟时钟测试；`tests/test_avatar_frontend.py` 将其纳入测试（需要 Node.js）。覆盖六轮空闲、眨眼独立性、讲话中断、姿势别名、快速切换、不同宽高比的定位及隐藏/减少动态效果。
- 验证：Node 语法检查通过；使用项目 `.venv/Scripts/python.exe -m unittest discover -s tests`，16 项全部通过。系统 Python 缺少 pypdf，不用于项目全量测试。浏览器查看六姿势闭眼预览，并对原图坐标及局部遮罩做图像复核。
- 未完成验证：尚未完成全部窄屏尺寸、镜像及说话状态的浏览器逐帧视觉复核；缩放定位已通过运行逻辑测试。临时诊断页面与图片已清理。

### 2026-09-15：走动三秒后站定，取消反复走动

- 需求：人物短暂走动约 3 秒后原地站住，避免一直来回走。
- 修改 `app/static/app.js`：保留单次 3 秒走动，每次页面加载后最多触发一次；走过之后从随机待机动作中排除走动。取消待机或切换姿势时保留横向位置，不再自动回到中央。
- 修改 `tests/avatar_runtime.cjs`：验证走动在 3 秒结束、取消待机后位置保留，以及后续六轮待机不再触发走动。
- 验证：`node tests/avatar_runtime.cjs` 通过；`python -m unittest discover -s tests -p test_avatar_frontend.py` 的 3 项测试全部通过。
- 未完成事项：未进行浏览器视觉验证。

### 2026-09-15：交替迈步与站定，去除整图走动浮动

- 需求：左右移动及转向时双脚有步伐变化，避免整张人物图上下漂浮；保留走 3 秒后站定且不反复走动。
- `app/static/app.js` 新增六步交替步态：支撑脚保持地面坐标，摆动脚抬起前移，按髋、膝、脚位置变形；上身以轻微水平变形表达转向，不整体升降。Canvas 使用双线性采样消除裤腿分段接缝；动画结束或中断时停止帧循环并保留当前位置。
- `app/static/index.html` 增加走动 Canvas；`app/static/styles.css` 移除整图呼吸浮动和走动上下弹跳，取消待机张望、摇摆中的横向平移。
- `tests/avatar_runtime.cjs` 验证左右方向支撑脚不滑动、双脚交替抬起、结束时收步、3 秒站定及后续不重复走动；`tests/test_avatar_frontend.py` 更新步态图层检查。
- 验证：Node 语法检查通过，前端 4 项测试通过。使用本机 Edge 无头浏览器检查步态截图，修复并复核裤腿接缝；左右方向运行、中途切换姿势和减少动态效果检查通过，无页面 JavaScript 错误。
- 限制：基于现有正面二维图片的腿部变形与轻微转向，并非具备侧面素材或三维骨骼的完整真人转身。

### 2026-09-15：气泡贴近数字人并随人物移动

- 需求：让对话气泡跟随数字人移动，呈现人物正在说话的效果。
- 修改 `app/static/index.html`：将气泡放入人物移动容器，随容器一起移动并保留停止后的位置。
- 修改 `app/static/styles.css`：气泡定位到头部右侧，尖角朝向人物；调整窄屏宽度和长文本换行。子气泡不再额外叠加舞台共享位移，避免双倍移动。
- 验证：项目虚拟环境执行前端测试，4 项通过；本地浏览器已确认气泡位于人物头部右侧。
- 未完成验证：未进行所有窄屏尺寸和走动全过程的逐帧视觉复核。

### 2026-09-15：气泡避让人物并精简显示内容

- 需求：气泡不遮挡数字人，每次少显示一些内容，尽量放在人物侧上方。
- `app/static/styles.css`：气泡底边固定在人像容器上方，内容向上展开；缩小宽度、内边距及尖角，尖角朝下，继续随人物容器移动。
- `app/static/app.js`：气泡只取第一句，最多显示 20 个字符（含省略号），完整回答仍保留在对话区。
- `app/static/index.html`：缩短初始问候。
- 验证：Node 语法检查通过；前端 4 项测试通过；额外执行首句提取、20 字上限及代码行提示保留检查，均通过；本地页面已加载新的简短问候。
- 未完成验证：尚未完成所有屏幕尺寸、姿势和走动全过程的视觉检查。

### 2026-09-15：分隔黑板与人物区域，气泡靠近头部右上侧

- 需求：气泡在人物侧上方且距离更近，同时不遮挡人物及黑板内容。
- `app/static/styles.css`：黑板统一占舞台顶部 44% 高度，人物容器位于底部 48% 区域；气泡在人物容器顶部靠右放置，用舞台高度与容器宽度计算头部侧向留白。尖角改为朝向左侧人物，收窄桌面气泡宽度，保留跟随位移及每次最多 20 字的既有逻辑。
- 窄屏黑板内容超出可视高度时可滚动查看，避免溢出到气泡区域。
- 验证：项目虚拟环境执行前端测试，4 项通过。
- 未完成验证：未完成浏览器各尺寸截图及所有姿势下的视觉检查。

### 2026-09-15：气泡分段显示，不再截断并添加省略号

- 需求：气泡每次可以少显示内容，但不能用省略号截掉后文。
- `app/static/app.js`：按标点或最多 20 字拆分气泡，依次显示全部分段，每段停留至少 2.2 秒，较长分段适当延长；最后一段保留。新提示到来取消旧分段，页面隐藏时暂停推进。
- 移除回答与语音入口的 64 字截断；浏览器语音使用当前朗读段落，纯文字回答保留完整分段队列。查找教材提示改用句号。
- `tests/avatar_runtime.cjs`：新增内容无损拼接、每段字数上限、逐段推进、末段保留和新提示取消旧队列检查。
- 验证：Node 语法检查通过；前端 4 项测试通过（包含新增分段运行逻辑检查）。
- 限制：分段按阅读时间切换，并非逐字语音字幕同步；新提示或黑板指引到来会替换当前气泡。未进行实际语音与浏览器轮播视觉复核。

### 2026-09-15：核实视频声音样本并让气泡实时跟随头部

- 用户确认声音所有者已授权使用视频声音。浏览器已打开科研前言视频列表；本地已有《1.课程介绍》30–47 秒参考音频、逐字稿及来源记录，已选为默认讲解样本。核验 WAV 为 17 秒、16 kHz 单声道，未重复导入。
- `app/static/app.js`：气泡按当前姿势的头部坐标与实际渲染尺寸逐帧定位，跟随横移、姿势动画及镜像变化；靠近舞台右边时移到左侧，限制舞台边界并避让黑板，隐藏页面停止跟随循环。
- `app/static/styles.css`：气泡位于人物左侧时翻转尖角方向。
- `app/static/app.js`：状态文字改为“已配置声音样本”，避免仅有样本就声称真人声音已就绪。
- `tests/avatar_runtime.cjs`：增加同位移跟随、右边界换侧及黑板避让验证。Node 语法检查通过，项目虚拟环境全量 18 项测试通过；浏览器已加载更新页面。
- 未完成：本机配置的 GPT-SoVITS 9880 端口未运行，尚未合成或试听目标真人音色；需部署/启动该服务。未完成全部窄屏和姿势的视觉复核。

### 2026-09-17：重建本地 Git 仓库

- 背景：项目为接手所得，原 `.git` 仅是无提交、无远程地址的空仓库，因此重新建立干净的版本历史。
- 重建仓库并将默认分支设为 `main`，使用当前项目内容建立初始提交。
- 修改 `.gitignore`：补充忽略 `data/logs/`，继续排除本地模型配置、教材文件、声音样本、生成音频、虚拟环境和 Python 缓存；保留必要的 `.gitkeep` 占位文件。
- 验证：确认旧仓库没有提交、分支历史或远程地址；重建后检查分支、提交记录和工作区状态。
- 未完成事项：尚未配置远程仓库；需由项目所有者决定 GitHub、GitLab 或其他远程地址。
