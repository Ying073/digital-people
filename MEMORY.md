# 项目记忆与改动记录

## 开始工作前必读

每次新对话或接手本项目时，先完整读取本文件，再开始修改。完成改动后及时补充记录，方便后续对话接续工作。项目根目录的 `AGENTS.md` 同时声明了这一协作要求。

## 项目背景

- 项目名称：西西编程伙伴。
- 项目用途：面向小学 C++ 启蒙课堂的卡通数字人网页应用，支持教材检索、问答、教师管理及语音播放。
- 启动入口：`./run.ps1`；后台启动：`./run.ps1 -Background`。
- 主要目录：`app/` 为应用代码，`data/` 为本地数据，`tests/` 为测试。
- 上述背景依据当前 `README.md` 整理；具体实现和配置须结合当前文件核实。

## 记录约定

- 按日期追加记录，说明改动目的、涉及文件、验证结果及未完成事项。
- 明确区分已完成、未验证和待处理内容。
- 不在本文件中保存密钥、密码或私人数据。
- 本记录自 2026-09-15 起建立；此前改动未逐项追溯。

## 改动记录

### 2026-09-15：建立项目记忆与交接规则

- 需求：在项目内记录改动，并要求新对话先读取记录再开始改动。
- 新增 `MEMORY.md`：整理项目背景，建立后续改动记录格式。
- 新增 `AGENTS.md`：要求开始修改前读取本文件，并在完成改动后同步追加记录。
- 验证：已核对 `README.md` 中的项目背景及启动入口；已检查两份文档内容。本次仅新增文档，未运行应用测试。
- 未完成事项：无。

### 2026-09-15：放慢空闲动作并校准面部效果

- 空闲首次触发和每次动作结束后的等待统一为随机 15–25 秒，移除语音结束及走动后的短间隔特例；眨眼保持独立的 3.2–6 秒等待和 170 毫秒闭眼。
- `app/static/app.js` 为六张实际使用的姿势素材配置原图坐标，别名按素材复用；用原图邻近肤色生成眼睛和嘴部遮罩，闭眼保留眼镜。面部效果按当前显示图片切换，并按 object-fit 的实际显示范围定位，随同人物镜像。
- `app/static/index.html`、`app/static/styles.css` 更新面部图层；姿势切换期间隐藏效果，减少动态效果时关闭面部动画。修复切换时的眨眼结束计时器残留，页面隐藏时停止调度。
- `tests/avatar_runtime.cjs` 新增实际 JS 调度/切换逻辑的模拟时钟测试；`tests/test_avatar_frontend.py` 将其纳入测试（需要 Node.js）。覆盖六轮空闲、眨眼独立性、讲话中断、姿势别名、快速切换、不同宽高比的定位及隐藏/减少动态效果。
- 验证：Node 语法检查通过；使用项目 `.venv/Scripts/python.exe -m unittest discover -s tests`，16 项全部通过。系统 Python 缺少 pypdf，不用于项目全量测试。浏览器查看六姿势闭眼预览，并对原图坐标及局部遮罩做图像复核。
- 未完成验证：尚未完成全部窄屏尺寸、镜像及说话状态的浏览器逐帧视觉复核；缩放定位已通过运行逻辑测试。临时诊断页面与图片已清理。

### 2026-09-15：走动三秒后站定，取消反复走动

- 需求：人物短暂走动约 3 秒后原地站住，避免一直来回走。
- 修改 `app/static/app.js`：保留单次 3 秒走动，每次页面加载后最多触发一次；走过之后从随机待机动作中排除走动。取消待机或切换姿势时保留横向位置，不再自动回到中央。
- 修改 `tests/avatar_runtime.cjs`：验证走动在 3 秒结束、取消待机后位置保留，以及后续六轮待机不再触发走动。
- 验证：`node tests/avatar_runtime.cjs` 通过；`python -m unittest discover -s tests -p test_avatar_frontend.py` 的 3 项测试全部通过。
- 未完成事项：未进行浏览器视觉验证。

### 2026-09-15：交替迈步与站定，去除整图走动浮动

- 需求：左右移动及转向时双脚有步伐变化，避免整张人物图上下漂浮；保留走 3 秒后站定且不反复走动。
- `app/static/app.js` 新增六步交替步态：支撑脚保持地面坐标，摆动脚抬起前移，按髋、膝、脚位置变形；上身以轻微水平变形表达转向，不整体升降。Canvas 使用双线性采样消除裤腿分段接缝；动画结束或中断时停止帧循环并保留当前位置。
- `app/static/index.html` 增加走动 Canvas；`app/static/styles.css` 移除整图呼吸浮动和走动上下弹跳，取消待机张望、摇摆中的横向平移。
- `tests/avatar_runtime.cjs` 验证左右方向支撑脚不滑动、双脚交替抬起、结束时收步、3 秒站定及后续不重复走动；`tests/test_avatar_frontend.py` 更新步态图层检查。
- 验证：Node 语法检查通过，前端 4 项测试通过。使用本机 Edge 无头浏览器检查步态截图，修复并复核裤腿接缝；左右方向运行、中途切换姿势和减少动态效果检查通过，无页面 JavaScript 错误。
- 限制：基于现有正面二维图片的腿部变形与轻微转向，并非具备侧面素材或三维骨骼的完整真人转身。

### 2026-09-15：气泡贴近数字人并随人物移动

- 需求：让对话气泡跟随数字人移动，呈现人物正在说话的效果。
- 修改 `app/static/index.html`：将气泡放入人物移动容器，随容器一起移动并保留停止后的位置。
- 修改 `app/static/styles.css`：气泡定位到头部右侧，尖角朝向人物；调整窄屏宽度和长文本换行。子气泡不再额外叠加舞台共享位移，避免双倍移动。
- 验证：项目虚拟环境执行前端测试，4 项通过；本地浏览器已确认气泡位于人物头部右侧。
- 未完成验证：未进行所有窄屏尺寸和走动全过程的逐帧视觉复核。

### 2026-09-15：气泡避让人物并精简显示内容

- 需求：气泡不遮挡数字人，每次少显示一些内容，尽量放在人物侧上方。
- `app/static/styles.css`：气泡底边固定在人像容器上方，内容向上展开；缩小宽度、内边距及尖角，尖角朝下，继续随人物容器移动。
- `app/static/app.js`：气泡只取第一句，最多显示 20 个字符（含省略号），完整回答仍保留在对话区。
- `app/static/index.html`：缩短初始问候。
- 验证：Node 语法检查通过；前端 4 项测试通过；额外执行首句提取、20 字上限及代码行提示保留检查，均通过；本地页面已加载新的简短问候。
- 未完成验证：尚未完成所有屏幕尺寸、姿势和走动全过程的视觉检查。

### 2026-09-15：分隔黑板与人物区域，气泡靠近头部右上侧

- 需求：气泡在人物侧上方且距离更近，同时不遮挡人物及黑板内容。
- `app/static/styles.css`：黑板统一占舞台顶部 44% 高度，人物容器位于底部 48% 区域；气泡在人物容器顶部靠右放置，用舞台高度与容器宽度计算头部侧向留白。尖角改为朝向左侧人物，收窄桌面气泡宽度，保留跟随位移及每次最多 20 字的既有逻辑。
- 窄屏黑板内容超出可视高度时可滚动查看，避免溢出到气泡区域。
- 验证：项目虚拟环境执行前端测试，4 项通过。
- 未完成验证：未完成浏览器各尺寸截图及所有姿势下的视觉检查。

### 2026-09-15：气泡分段显示，不再截断并添加省略号

- 需求：气泡每次可以少显示内容，但不能用省略号截掉后文。
- `app/static/app.js`：按标点或最多 20 字拆分气泡，依次显示全部分段，每段停留至少 2.2 秒，较长分段适当延长；最后一段保留。新提示到来取消旧分段，页面隐藏时暂停推进。
- 移除回答与语音入口的 64 字截断；浏览器语音使用当前朗读段落，纯文字回答保留完整分段队列。查找教材提示改用句号。
- `tests/avatar_runtime.cjs`：新增内容无损拼接、每段字数上限、逐段推进、末段保留和新提示取消旧队列检查。
- 验证：Node 语法检查通过；前端 4 项测试通过（包含新增分段运行逻辑检查）。
- 限制：分段按阅读时间切换，并非逐字语音字幕同步；新提示或黑板指引到来会替换当前气泡。未进行实际语音与浏览器轮播视觉复核。

### 2026-09-15：核实视频声音样本并让气泡实时跟随头部

- 用户确认声音所有者已授权使用视频声音。浏览器已打开科研前言视频列表；本地已有《1.课程介绍》30–47 秒参考音频、逐字稿及来源记录，已选为默认讲解样本。核验 WAV 为 17 秒、16 kHz 单声道，未重复导入。
- `app/static/app.js`：气泡按当前姿势的头部坐标与实际渲染尺寸逐帧定位，跟随横移、姿势动画及镜像变化；靠近舞台右边时移到左侧，限制舞台边界并避让黑板，隐藏页面停止跟随循环。
- `app/static/styles.css`：气泡位于人物左侧时翻转尖角方向。
- `app/static/app.js`：状态文字改为“已配置声音样本”，避免仅有样本就声称真人声音已就绪。
- `tests/avatar_runtime.cjs`：增加同位移跟随、右边界换侧及黑板避让验证。Node 语法检查通过，项目虚拟环境全量 18 项测试通过；浏览器已加载更新页面。
- 未完成：本机配置的 GPT-SoVITS 9880 端口未运行，尚未合成或试听目标真人音色；需部署/启动该服务。未完成全部窄屏和姿势的视觉复核。

### 2026-09-17：重建本地 Git 仓库

- 背景：项目为接手所得，原 `.git` 仅是无提交、无远程地址的空仓库，因此重新建立干净的版本历史。
- 重建仓库并将默认分支设为 `main`，使用当前项目内容建立初始提交。
- 修改 `.gitignore`：补充忽略 `data/logs/`，继续排除本地模型配置、教材文件、声音样本、生成音频、虚拟环境和 Python 缓存；保留必要的 `.gitkeep` 占位文件。
- 验证：确认旧仓库没有提交、分支历史或远程地址；重建后检查分支、提交记录和工作区状态。
- 未完成事项：尚未配置远程仓库；需由项目所有者决定 GitHub、GitLab 或其他远程地址。

### 2026-09-17：连接 GitHub 远程仓库

- 将 `https://github.com/Ying073/digital-people.git` 配置为本地 Git 远程 `origin`，fetch 与 push 使用同一地址。
- 验证：`git ls-remote origin` 执行成功；远程仓库当前为空。
- 未完成事项：尚未将本地 `main` 分支推送至 GitHub。

### 2026-09-17：重建开发环境并加固启动检查

- 发现旧 `.venv` 仍引用接手前机器上的 `D:\APP\python312\python.exe`，无法运行；使用本机 Python 3.13.5 重新创建项目虚拟环境并安装 `requirements.txt` 中锁定的依赖。
- 修改 `run.ps1`：启动前实际执行虚拟环境 Python 健康检查；失效时只删除经过绝对路径校验的项目 `.venv` 并重建；依赖缺失时自动安装，安装失败立即停止并给出明确错误。
- 修改 `run.ps1`：后台启动改为最多等待 10 秒，并通过 `/api/status` 判断应用是否就绪或已经运行，不再依赖可能受权限限制的 Windows TCP 监听表。
- 验证：项目虚拟环境为 Python 3.13.5；`pip check` 无依赖冲突；Python 全量 18 项测试通过；`app.js`、`admin.js` Node 语法检查及 `avatar_runtime.cjs` 运行测试通过；后台服务可在 `http://127.0.0.1:8000` 访问，状态接口返回 1 份资料和 74 个知识片段。
- 未完成事项：仍使用未设置 `ADMIN_PASSWORD` 时的开发默认密码；正式部署前必须配置强密码。GPT-SoVITS 9880 服务仍需单独部署和启动。

### 2026-09-18：修复模型查重回退后的模式标记

- 问题：模型答案连续两次与教材片段过度重合时，后端会改用本地规则改述，但随后无条件把响应 `mode` 覆盖为 `model`，导致前端错误显示模型讲解提示。
- 修改 `app/main.py`：先将成功模型回答标记为 `model`；若二次查重仍不通过并回退本地改述，则保留 `local_rephrase`。
- 新增 `tests/test_chat_api.py` 回归测试，模拟两次模型回答都照抄教材，验证最终响应模式为 `local_rephrase`。测试在修复前按预期失败，修复后通过。
- 验证：项目虚拟环境全量 19 项 Python 测试通过；前端两个 JavaScript 文件语法检查和数字人动画运行测试通过；服务重新启动，`/api/status` 正常返回 1 份资料和 74 个知识片段。
- 未完成事项：本次只修复响应模式标记，不改变模型查重阈值或本地改述内容。

### 2026-09-18：支持拖动数字人并优化动作自然度

- `app/static/app.js` 为数字人增加鼠标与触屏指针拖动：拖动范围限制在舞台内，松手后保留位置，拖动时暂停待机动作，并让对话气泡继续跟随人物。
- `app/static/styles.css` 增加抓取光标、轻微随动倾斜和松手回稳动画；收敛挥手、点头、思考、说话、庆祝及纠正动作的位移和旋转幅度，减少夸张跳动。
- `app/static/index.html` 为前端脚本和样式增加版本参数，避免本地预览继续使用旧缓存。
- `tests/avatar_runtime.cjs` 和 `tests/test_avatar_frontend.py` 增加拖动边界、位置保持、倾斜回稳、指针事件、触屏样式和资源版本检查。
- 验证：项目虚拟环境全量 21 项 Python 测试通过；`app.js`、`admin.js` Node 语法检查和数字人动画运行测试通过；`git diff --check` 通过；`/api/status` 正常返回 1 份资料和 74 个知识片段。已在本地应用浏览器中实际把数字人向右上拖动，确认位置保持、气泡跟随、松手状态复原且未越出舞台。
- 未完成事项：当前位置仅在当前页面生命周期内保留，刷新页面后会恢复默认位置；尚未增加跨刷新持久化。

### 2026-09-18：增加桌面宠物式自主互动

- `app/static/app.js` 将空闲动作调整为约 6–10 秒触发一次，缩短单次动作时间，并允许人物多次自主散步；散步会根据舞台剩余空间自动转向，保持在可见边界内。
- 增加指针关注与轻点回应：鼠标靠近时人物会轻微朝向指针，轻点人物会触发一次短促弹跳；拖拽、讲解、朗读、页面隐藏和系统“减少动态效果”仍会阻止或停止自主动作。
- `app/static/styles.css` 新增关注姿态和宠物弹跳动画；继续避免持续的整个人物上下漂浮。`app/static/index.html` 更新静态资源版本参数，确保预览加载新版。
- `tests/avatar_runtime.cjs` 与 `tests/test_avatar_frontend.py` 覆盖关注、轻点回应、6–10 秒调度、重复散步、舞台边缘自动回头及交互事件绑定。
- 验证：项目虚拟环境全量 22 项 Python 测试通过；前端 JavaScript 语法检查、动画运行测试和 `git diff --check` 通过。已在本地应用浏览器中实测轻点后的“开心回应”和自主“轻轻活动”，浏览器控制台无错误。
- 未完成事项：当前为二维图片与轻量步态模拟，动态丰富度仍受现有姿势素材限制；若要达到带骨骼的完整桌宠效果，需要补充多方向帧或 Live2D/Spine 素材。

### 2026-09-18：增强人物舞台立体感

- `app/static/index.html` 在人物移动容器内增加独立脚下阴影层，并更新静态资源版本参数。
- `app/static/styles.css` 增加舞台聚光渐变、地面层次、850px 透视、脚下软阴影和人物三层投影；轻点弹跳时阴影会同步收缩，使人物与地面建立空间关系。
- `app/static/app.js` 将鼠标关注位置映射为受限的 X/Y 轴视差（最大约 2.4°/3.2°），离开人物后平滑归零，同时让阴影轻微反向偏移以强化纵深。
- `tests/avatar_runtime.cjs` 与 `tests/test_avatar_frontend.py` 增加阴影结构、透视样式、聚光渐变、视差角度边界和归零行为检查。
- 验证：项目虚拟环境全量 23 项 Python 测试通过；前端 JavaScript 语法检查、动画运行测试、`git diff --check` 与 `/api/status` 检查通过。浏览器实测脚下阴影为约 122×28 像素、850px 透视和三层人物投影均生效，轻点互动正常且控制台无错误。
- 限制：本次是在现有二维透明 PNG 上增加光影和轻量视差，并非真实三维模型；更大角度转身仍需要多方向素材或骨骼模型。

### 2026-09-18：支持用户调整人物区与问答区宽度

- `app/static/index.html` 在人物舞台与问答区之间增加可聚焦的竖向分隔条，使用 `role="separator"` 和动态 ARIA 数值提供无障碍语义。
- `app/static/styles.css` 将桌面布局改为四列网格，分隔条悬停、聚焦或拖动时高亮；人物区最小 300px，问答区至少保留约 370–380px。840px 以下隐藏分隔条并继续使用上下布局。
- `app/static/app.js` 支持鼠标和触屏拖动、方向键每次 20px 调整以及 Home/End 边界跳转；释放后将人物区宽度保存到 `localStorage`，刷新和窗口尺寸变化时重新按可用边界应用。
- `tests/avatar_runtime.cjs` 与 `tests/test_avatar_frontend.py` 增加宽度夹取、拖动、持久化、ARIA 属性、键盘事件和窄屏隐藏检查。
- 验证：项目虚拟环境全量 24 项 Python 测试通过；前端 JavaScript 语法检查、动画运行测试、`git diff --check` 与 `/api/status` 检查通过。浏览器实测分隔条从 456px 拖到 386px、再拖到 496px，两侧实时重排；刷新后仍保持 496px，控制台无错误。
- 未完成事项：宽度偏好保存在当前浏览器本地，不会跨浏览器或跨设备同步。

### 2026-09-18：建立可审核的自更新知识库闭环

- 安全策略：对话中教材检索为空或最高相关度低于阈值的问题只进入“待审核候选”，不会自动影响后续回答；教师必须在密码保护的管理后台核对、编辑并明确发布，才能成为正式补充知识。
- 新增 `app/learning.py`：使用原子写入和线程锁维护本地 JSON 学习库；归一化并合并重复问题、累计出现次数、保存首次/最近出现时间，支持审核通过、忽略及已审核知识检索。只保存问题与候选答案，不保存完整对话历史，并对明显邮箱、手机号和长号码做脱敏；问候、致谢等短消息不会进入队列。
- 修改 `app/main.py`、`app/style.py`：合并教材与教师审核知识的检索结果；低置信度问题在回答后进入审核队列；新增待审核列表、发布和忽略接口；无外部模型时也会直接采用匹配的教师标准答案。接口响应会标明知识缺口是否已记录及来源类型。
- 修改 `app/static/admin.html`、`app/static/admin.js`、`app/static/admin.css`：管理页新增“知识缺口审核”，展示重复次数和最近出现时间，允许编辑标准答案、确认发布或忽略；发布与忽略均有二次确认，并为管理端静态资源增加版本参数。
- 修改 `.gitignore`：忽略运行期 `data/learning_store.json`，避免用户问题和审核知识进入 Git。新增实施计划 `docs/superpowers/plans/2026-09-18-self-updating-knowledge-base.md`。
- 测试：新增 `tests/test_learning.py`、`tests/test_admin_frontend.py` 并扩展 `tests/test_chat_api.py`，覆盖重复归并、持久化、脱敏、无效消息过滤、审核状态、审核知识检索、低/高置信度判断、管理接口与本地回答闭环。测试均先观察到预期失败后再实现。
- 验证：项目虚拟环境全量 35 项 Python 测试通过；`app.js`、`admin.js` Node 语法检查、数字人动画运行测试和 `git diff --check` 通过；重启服务后 `/api/status` 返回 1 份资料和 74 个知识片段，审核接口返回 0 条待审核。浏览器实测管理页新版审核区正常显示、控制台无错误，未写入或发布测试候选。
- 限制：这是通过检索增强实现的“知识增长”，不会自动训练或修改大模型权重；缺口判断暂用最高检索分数阈值，后续可结合用户反馈和语义向量校准；JSON 存储适合当前单机课堂版本，大规模多用户部署应迁移到数据库并增加租户隔离、审计和备份。

### 2026-09-19：部署 GPT-SoVITS 并打通老师声音零样本克隆

- 新建分支 `codex/gpt-sovits-voice`。增加 `scripts/setup-gpt-sovits.ps1` 和 `scripts/start-gpt-sovits.ps1`：第三方源码与模型安装到忽略提交的 `.local/GPT-SoVITS/`，使用独立 Conda Python 3.10 环境、PyTorch 2.7.1+cu128，并仅监听 `127.0.0.1:9880`。为 Windows 中文运行路径处理上游 `torchcodec`、`pyopenjtalk`、`jieba_fast` 和源码版 OpenCC 的安装兼容问题；当前运行时不包含日语 Open JTalk 支持。
- 实际安装官方 GPT-SoVITS 修订 `48b1a0169a28582a8984402f82cf438d3bfa6aca`；RTX 5060 Laptop GPU 被 CUDA 12.8 正确识别，模型加载后约占用 4.4 GB 显存，`/docs` 返回 HTTP 200。
- 原授权录音为 17 秒，超出 GPT-SoVITS 官方接口要求的 3–10 秒参考范围。从原录音 2.9–10.3 秒派生 7.4 秒中文参考片段，并使用与片段一致的逐字稿设为默认讲解声音；原授权录音仍保留。声音数据和生成缓存均继续由 `.gitignore` 排除。
- `app/tts.py` 将合成等待时间从 45 秒提高到 180 秒；`app/main.py` 增加受教师密码保护的语音服务状态接口。管理页显示 GPT-SoVITS 是否运行、服务地址和启动命令，并明确提示使用 3–10 秒清晰授权录音与准确逐字稿。`README.md` 增加一键安装和启动说明。
- 实测首次三句合成约 105 秒，热身后生成“你好，我是西西老师。我们一起学习编程。”约 3.3 秒；输出为 2.60 秒、32 kHz、单声道、16-bit RIFF/WAV（166444 字节）。课堂服务 `/api/status` 正常，管理端语音状态返回运行中。
- 验证：Python 全量 41 项测试通过；`app.js`、`admin.js` Node 语法检查、数字人动画运行测试和 `git diff --check` 均通过。
- 未完成事项：当前是预训练模型的零样本声音克隆，尚未用约 1 分钟干净录音微调专属模型；长文本仍应按句切分并预留首次模型热身时间。正式部署前仍需设置强 `ADMIN_PASSWORD`。

### 2026-09-19：统一使用短参考声音并清理旧样本

- 将 7.4 秒、逐字稿结尾为“在网上和大家一起。”的授权参考音频设为默认、鼓励、纠错和庆祝声音；所有现有语气均使用这一个样本。
- 按用户要求从本地声音库删除旧的 17 秒参考 WAV 及其清单项；声音库现仅保留 1 个有效样本。旧样本文件已删除，若需要恢复，仍可根据此前记录的来源视频重新截取。
- 验证：管理接口返回 1 个声音样本且默认 ID 与剩余样本一致；不指定样本调用试听接口成功生成 213804 字节 WAV，GPT-SoVITS 服务保持运行。
- 未完成事项：管理页面刷新后才会移除旧选项；正式部署前仍需设置强 `ADMIN_PASSWORD`。
